#include <stdio.h>
#include <stdbool.h>  //used to include boolean data type

int main() {

    //AND && checks for conditions on both sides needs to be true
    //OR || checks for conditions on either side needs to be true
    //NOT ! returns opposite for the condition
    //Boolean(true or false)

    /*
    ternary operator is a shortcut of if else 
    when assigning/returning a value

    (condition) ? value if true : value if false
    */

    return 0;
}